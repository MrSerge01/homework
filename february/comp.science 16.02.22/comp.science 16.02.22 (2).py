a = int(input("Number of bytes: "))
b = a*1024
print(b, "kilobytes")

# context: this was a task for me to code this, finds how many bytes are in a kilobyte )
# teacher is dumb so he flipped the names away
